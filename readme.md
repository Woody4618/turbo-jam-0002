To build and run:

```bash
turbo run -w angry-bonk
```

```bash
ctrl + r to reload
```

## Multiplayer

```bash
turbo host . -p 2
```

get session id:

other client:

```bash
turbo connect sessionId
```
